apiVersion: v1
kind: Service
metadata:
  name: event-service
  labels:
    app: event-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: event-service

  